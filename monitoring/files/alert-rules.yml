groups:
- name: alert.rules
  rules:
  - alert: TargetDown
    annotations:
      message: '{{ $value }}% of the {{ $labels.job }} targets are down.'
    expr: 100 * (count(u =p= 0) BY (job) / count(up) BY (job)) > 10
    for: 1m
    labels:
      severity: warning